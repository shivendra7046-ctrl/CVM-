CVM++
A custom scripting language and stack-based virtual machine developed in C++.

CVM++ demonstrates the complete workflow of a programming language implementation, beginning from source code and ending with execution on a custom virtual machine.

The project includes:

Lexical Analysis (Lexer)
Syntax Analysis (Parser)
Abstract Syntax Tree (AST)
Bytecode Compilation
Stack-Based Virtual Machine
Script Execution from .cvm files
Architecture
Source Code (.cvm)

↓

Lexer

↓

Parser

↓

Abstract Syntax Tree

↓

Bytecode Compiler

↓

Virtual Machine

↓

Program Output

Features
Lexer
The lexer scans source code and converts it into tokens.

Example:

var x = 10;
Generated Tokens:

VAR
IDENTIFIER
ASSIGN
NUMBER
SEMICOLON
Parser
The parser converts tokens into an Abstract Syntax Tree (AST).

Example AST:

VAR_DECL -> x=10
PRINT -> x
ASSIGN -> x=50
Bytecode Compiler
The compiler converts AST nodes into low-level bytecode instructions understood by the virtual machine.

Example:

PUSH 10
STORE x

LOAD x
PRINT
Virtual Machine
The virtual machine executes bytecode instructions using a stack-based execution model.

Supported Instructions:

PUSH
STORE
LOAD
ADD
PRINT
Supported Language Features
Variable Declaration
var x = 10;
Variable Assignment
x = 50;
Integer Values
var score = 100;
Arithmetic Addition
print(x+y);
Print Statements
print(x);
Example Program
var x = 10;
var y = 20;

print(x);
print(y);

print(x+y);

x = 50;

print(x);

var a = 100;
var b = 25;

print(a);
print(b);

print(a+b);
Example Output
10
20
30
50
100
25
125
Example AST
VAR_DECL -> x=10
VAR_DECL -> y=20

PRINT -> x
PRINT -> y

PRINT -> x+y

ASSIGN -> x=50

PRINT -> x

VAR_DECL -> a=100
VAR_DECL -> b=25

PRINT -> a
PRINT -> b

PRINT -> a+b
Example Bytecode
PUSH 10
STORE x

PUSH 20
STORE y

LOAD x
PRINT

LOAD y
PRINT

LOAD x
LOAD y
ADD
PRINT

PUSH 50
STORE x

LOAD x
PRINT

PUSH 100
STORE a

PUSH 25
STORE b

LOAD a
PRINT

LOAD b
PRINT

LOAD a
LOAD b
ADD
PRINT
Project Structure
CVM++
│
├── src
│   ├── lexer.h
│   ├── lexer.cpp
│   ├── parser.h
│   ├── parser.cpp
│   ├── compiler.h
│   ├── compiler.cpp
│   ├── vm.h
│   ├── vm.cpp
│   └── main.cpp
│
├── tests
│   ├── hello.cvm
│   └── arithmetic.cvm
│
├── README.md
├── .gitignore
└── .gitattributes
Build Instructions
Compile:

g++ src/main.cpp src/lexer.cpp src/parser.cpp src/compiler.cpp src/vm.cpp -o cvm
Run:

.\cvm.exe
Execution Stages
When the program is executed, CVM++ displays:

Generated Tokens
Generated AST
Generated Bytecode
Final Program Output
This allows easy debugging and visualization of every stage of the compilation process.

Technologies Used
C++
Object Oriented Programming
Lexical Analysis
Parsing
AST Construction
Bytecode Compilation
Stack-Based Virtual Machine Design
Future Enhancements
Planned extensions:

Subtraction
Multiplication
Division
Conditional Statements
Loops
Functions
String Support
Arrays
User Input
Error Handling
Interactive REPL
Author

Shivendra

B.Tech Electronics and Electrical Engineering

Indian Institute of Technology Guwahati

License
This project was developed for educational and academic purposes.
